// Uses the DOM with JavaScript/JQuery
function showReview(elid) {
    document.getElementById(elid).style.display = 'block';
};

function hideReview(elid){
    document.getElementById(elid).style.display='none';
};